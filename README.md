# fireuser
This project implements a user authentication system using Flutter integrated with Firebase Authentication. The application allows users to register and log in using their email and password. It securely validates user credentials and manages authentication states in real time.
